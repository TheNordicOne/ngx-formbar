{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgxFormbarBlock",
  "title": "Scaffold Block",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the component that this control will use. Defaults to the key if not provided.",
      "type": "string"
    },
    "key": {
      "description": "The name of the key that this control will be associated with.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the component.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
      }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "interfaceSuffix": {
      "type": "string",
      "description": "The suffix of the interface. Defaults to 'BlockConfig'",
      "default": "BlockConfig"
    },
    "componentSuffix": {
      "type": "string",
      "description": "The suffix of the component. Defaults to 'Block'",
      "default": "Block"
    },
    "interfaceFileSuffix": {
      "type": "string",
      "description": "Segment before '.ts' in the interface file name (e.g. 'type' -> name.type.ts). Defaults to 'type' only when the component has a type suffix."
    },
    "skipRegistration": {
      "type": "boolean",
      "description": "Skip automatic registration"
    },
    "viewProviderHelperPath": {
      "type": "string",
      "description": "Path to the viewProvider helper"
    },
    "schematicsConfig": {
      "type": "string",
      "description": "Path of the configuration that is to be used by this schematic"
    }
  },
  "required": [
    "key"
  ]
}

