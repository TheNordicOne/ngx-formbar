import { NgDocCategory } from '@ng-doc/core';
import ReactiveFormsCategory from '../ng-doc.category';

const ReactiveFormsGuidesCategory: NgDocCategory = {
  title: 'Guides',
  category: ReactiveFormsCategory,
  order: 2,
  expandable: true,
};

export default ReactiveFormsGuidesCategory;
