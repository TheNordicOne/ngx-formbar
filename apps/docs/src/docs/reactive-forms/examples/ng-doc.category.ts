import { NgDocCategory } from '@ng-doc/core';
import ReactiveFormsCategory from '../ng-doc.category';

const ReactiveFormsExamplesCategory: NgDocCategory = {
  title: 'Examples',
  category: ReactiveFormsCategory,
  order: 3,
  expandable: true,
};

export default ReactiveFormsExamplesCategory;
