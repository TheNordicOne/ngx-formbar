import { NgDocCategory } from '@ng-doc/core';
import ReactiveFormsExamplesCategory from '../ng-doc.category';

const ReactiveFormsInteractiveCategory: NgDocCategory = {
  title: 'Interactive',
  category: ReactiveFormsExamplesCategory,
  order: 1,
  expandable: true,
};

export default ReactiveFormsInteractiveCategory;
