{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Ngx Formwork Discovery Builder",
  "description": "Discovers components using Ngx Formwork directives",
  "type": "object",
  "properties": {
    "include": {
      "type": "array",
      "description": "Glob patterns to include when searching for components",
      "default": ["**/*.ts"],
      "items": {
        "type": "string"
      }
    },
    "exclude": {
      "type": "array",
      "description": "Glob patterns to exclude when searching for components (node_modules and spec files will always be excluded",
      "default": [],
      "items": {
        "type": "string"
      }
    },
    "outputPath": {
      "type": "string",
      "description": "Path where to save the discovered components information",
      "default": "./control-registrations.ts"
    },
    "configurationPath": {
      "type": "string",
      "description": "Path of the configuration that is to be used by this builder",
      "default": "./ngx-formwork.config.json"
    }
  },
  "additionalProperties": false
}

